class CardData {
  final String title;
  final String address;
  final String image;
  final double rate;
  final double distance;

  CardData(this.title, this.address, this.image, this.rate, this.distance);
}
